// This program is to test the syntax parser of the compiler.

Int main () {
  Bool t = True;

  Int j[11];
  for (Int i = 0; i < 10; i = i + 1) {
    j[i] = square(i);
    j[i + 1] = 2 * square(i);
  }

  return 0;
}

Int square (Int x) {
  return x * x;
}
