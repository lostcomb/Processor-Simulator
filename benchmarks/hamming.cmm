void main () {
  int h = hamming(10);
}

int hamming (int n) {
  int h = 0;
  while (n > 0) {
    h = h + mod(n, 2);
    n = n / 2;
  }
  return h;
}

int mod (int a, int b) {
  int div = a / b;
  return a - (div * b);
}
